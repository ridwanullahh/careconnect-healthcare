# Bismillah Ar-Rahman Ar-Raheem.
# Production environment (loaded by Vite during `vite build`).
# These values are baked into the frontend bundle at build time.
VITE_DB_MODE=api
VITE_API_BASE_URL=/api
VITE_DEV_SERVER_HTTPS=false
