# Development-mode overrides (loaded by Vite in dev mode, overrides .env)
# Bismillah Ar-Rahman Ar-Raheem.
# Sandbox gateway uses HTTP; HTTPS must be off.
VITE_DEV_SERVER_HTTPS=false
VITE_DB_MODE=api
VITE_API_BASE_URL=/api
